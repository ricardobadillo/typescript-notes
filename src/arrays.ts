// Arreglos con múltiples tipos de valores.
let array: (number | string)[];

array = [1, 2, 3, "4"];

// Arreglos con múltiples tipos de valores utilizando los genéricos.
let arreglo: Array<string | number>;

arreglo = ['1', 2, 3, 4];